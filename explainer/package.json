{
  "name": "capability-explainer",
  "private": true,
  "type": "module",
  "description": "Isolated whiteboard hand-draw explainer video pipeline. Not part of the capability-model SSOT tooling.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bundle": "node build.mjs",
    "draft": "node render.mjs --draft",
    "render": "node render.mjs",
    "timing": "node timing.mjs"
  },
  "dependencies": {
    "perfect-freehand": "^1.2.2",
    "roughjs": "^4.6.6"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "playwright-core": "^1.47.2"
  }
}
